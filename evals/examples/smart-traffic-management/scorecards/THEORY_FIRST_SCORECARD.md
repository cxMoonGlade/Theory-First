# Theory First arm scorecard

- Snapshot: 2026-07-15
- Arm artifact: [`THEORY_FIRST_PACKET.md`](../THEORY_FIRST_PACKET.md)
- Scoring lens: [`PRIMARY_AXES.md`](../PRIMARY_AXES.md)

This is the durable record of a separate read-only audit pass. It was written
after the arm ran. The repository does not contain the raw agent transcript, so
the run's claimed blindness is not independently replayable from this file.

## Axis A — evidence illusion: `CONTROLLED`

### Surviving-event ledger

| Event | Locator | Severity | Disposition |
|---|---|---|---|
| none | closure status `OPEN` and preregistration status `CODE_BLOCKED` | — | no unsupported premise entered a go decision |

### Caught-hazard ledger

The stable event identifiers and full dispositions are in the packet's
[`Illusion and drift trip ledger`](../THEORY_FIRST_PACKET.md#illusion-and-drift-trip-ledger).

| ID | Tempting illusion | Catch mechanism |
|---|---|---|
| I1 | queue-stability theorem → 10% person-delay benefit | source-claim/project-inference split; E5 remains contradicted |
| I2 | average pedestrian delay → P95 crossing wait | observable crosswalk; E2 remains missing |
| I3 | vehicle `timeLoss` or ride `waitingTime` → person/crossing delay | official-field semantic audit; E2 remains missing |
| I4 | scenario/default fixed timing → recalibrated comparator | baseline provenance row E3 remains missing |
| I5 | original Q-MP → composite MP without versioning | intervention variant map; E1 remains inference-only |
| I6 | live SUMO docs → release-pinned fidelity | implementation row E6 remains missing |
| I7 | Liu prose `lambda=0.0006` → copied tuning value | prose/grid/figure conflict retained in the source audit |
| I8 | unrelated `arXiv:2103.01115` → RESCO evidence | title/body identity check rejects the discovery association |
| I9 | favorable source result → city-pilot authority | external-validity row E9 remains missing |

Two non-load-bearing reading-note errors were caught by audit and corrected: a
same-run seed statement had been overread as ten replications, and an internal
“3-hour” versus “7–9 a.m.” conflict had initially been omitted. Neither had
entered E1–E9 or the gate result.

## Axis B — decision drift: `ANCHORED`

### Surviving-event ledger

| Event | Locator | Severity | Disposition |
|---|---|---|---|
| none | packet E1–E9 and the preregistration gate | — | exact controller and open bridges stop claim v1 instead of changing it |

The prompt does not freeze original vehicle-only Q-MP. The arm preserves that
ambiguity: E1 leaves the exact executable treatment `inference-only` and no
protocol is emitted. One non-event watch item remains: some disconfirmation text
specializes the unspecified incident to a capacity-reducing incident. Because
the incident stays a missing local input and no protocol is created, it does not
propagate into the decision.

## Secondary rubric and cost

- Safeguards: **8 explicit / 2 partial / 0 absent**.
- Search ledger: 22 entries.
- Close reading: four primary papers plus one official SUMO documentation set.
- Core artifacts: landscape, closure packet, preregistration gate, and five
  source audits.
- Wall-clock time was not recorded.

## Audit boundary

This score establishes neither scientific correctness nor a general
hallucination/drift rate. The primary axes were added after the landscape map
existed, and the score comes from one qualitative task.
